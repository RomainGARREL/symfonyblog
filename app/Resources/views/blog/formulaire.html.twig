{#{% form_theme form 'bootstrap_3_layout.html.twig' 'ckeditor.html.twig'%}#}
{% form_theme form 'ckeditor.html.twig'%}
{% for message in app.session.flashbag.get('Erreur') %}
    <p class="alert alert-success">{{message}}</p>
{% endfor %}
{{form_start(form)}}

{{form_label(form.titre)}}
{#{{form_errors(form.titre)}}#}
{{ form_widget(form.date,{'attr':{'class':'js-datepicker'}} ) }}  {#Et du con cf1 dans ajouter.html.twig#}

{#{{form_row(form.date)}}#}
{#{{form_widget(form)}} //va nous placer tt les fields definit dans l'ordre des methodes add#}
{{form_end(form)}} // affiche la fermeture de la balise form,
